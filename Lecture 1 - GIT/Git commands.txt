git clone https://github.com/richfrankrepo/CS50W.git - clone a repo to local

git add [filename or pattern]

git branch - show all branches

git branch [newbranchname]
git checkout [branch name] - switch to new a new branch
or git checkout -b to create and switch to the new branch at the same time.
git branch -d [branchname] remove the local branch


