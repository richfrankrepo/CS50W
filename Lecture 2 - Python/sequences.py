# sequences in python are like arrays

name = "Richard" 
print(name[0])


names = ["Richard","Nikki","Bella"] # lists are written in square brackets and are changeable
print(names[1])

ages = (46,41,11) # Tuples are like lists but written in curved brackets and are not unchangeable
print(ages[0])

thisset = {"apple","banana","orange"} # unordered list which cannot be index or changed, but can be added too
print(thisset)
thisset.add("pear")

# a dictionary is a key/value pair
myDictionary {
    "brand":"Honda"
    "model":"Civic"
    "year":"2013"
}